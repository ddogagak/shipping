"use client";
import Link from "next/link"; import {useState} from "react";
export default function PurchaseImport(){const[file,setFile]=useState<File|null>(null),[orders,setOrders]=useState<any[]>([]),[msg,setMsg]=useState(""); const run=async(mode:string)=>{if(!file)return setMsg("订单数据.xlsx 파일을 선택해줘."); setMsg(mode==="preview"?"분석 중...":"저장 중..."); const fd=new FormData();fd.append("file",file);fd.append("mode",mode);const res=await fetch("/api/purchases/import",{method:"POST",body:fd});const j=await res.json();if(!res.ok)return setMsg(j.message||"실패");if(mode==="preview"){setOrders(j.orders||[]);setMsg(`${j.orders?.length||0}개 주문을 찾았어.`)}else{setMsg(`${j.saved}개 주문 저장 완료`);}};
return <main style={{maxWidth:1100,margin:"0 auto",padding:24}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}><div><h1>매입 엑셀 등록</h1><p style={{color:"#666"}}>Taobao 订单数据.xlsx → 매입 DB</p></div><Link href="/purchases">← 매입관리</Link></div><section style={box}><input type="file" accept=".xlsx,.xls" onChange={e=>setFile(e.target.files?.[0]||null)}/><div style={{display:"flex",gap:8,marginTop:14}}><button style={btn} onClick={()=>run("preview")}>미리보기</button><button style={btn} onClick={()=>run("save")}>DB에 저장</button></div><p>{msg}</p></section>{orders.map(o=><section key={o.order_number} style={card}><b>{o.order_number}</b> · {o.shop_name} · {o.ordered_at}<div style={{marginTop:8}}>{o.items.map((i:any,n:number)=><div key={n} style={{padding:"7px 0",borderTop:"1px solid #ddd"}}>{i.product_name}<br/><small>{i.option_text}</small><b style={{float:"right"}}>¥{i.unit_price} × {i.quantity}</b></div>)}</div></section>)}</main>}
const box={border:"2px solid #0047FF",borderRadius:16,padding:20,background:"#FEFF5A"}; const card={border:"1px solid #ddd",borderRadius:14,padding:16,marginTop:12,background:"white"}; const btn={padding:"10px 16px",borderRadius:10,border:"1px solid #111",fontWeight:800,cursor:"pointer"};
